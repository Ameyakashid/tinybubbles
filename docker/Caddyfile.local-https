# LAN-only HTTPS setup. Devices must trust Caddy's local root CA before
# Tiny Bubbles clients will accept this certificate.
{$TINYBUBBLES_CLOUD_DOMAIN} {
	tls internal
	reverse_proxy tinybubbles-cloud:8787
}
