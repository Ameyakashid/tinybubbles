# Mindwtr Unreleased

Changes collected after `v1.1.0` and before the next version tag.

## Highlights

_None yet._

## Full Change List

- The desktop calendar's Week timeline can now show between two and seven days, stepped from a control that appears in the header while Week is active — the same adjustable day count mobile already has. Fewer days means wider columns, so longer event titles stay readable and dropping a task on a time slot is an easier target. The window keeps to the week it belongs to and holds the day you are on, so five days starting Monday stays Monday–Friday as you page through weeks. Day, Month and Schedule are unaffected, and the count is remembered per device rather than synced — a laptop and a wide monitor can each keep their own. (#951)
- Switched mobile local storage to a native SQLite engine (op-sqlite): SQL now executes on a dedicated native thread with a faster call path, the first step of moving storage and sync work off the UI thread on large libraries. Existing data is picked up in place; no migration.
- Finishing or editing a task on mobile no longer waits for the full-library backup snapshot: saves complete after the database write, and the backup and widget refresh land coalesced in the background (bursts of saves now write one backup instead of one each — a single save had spent up to 12 seconds on this with large libraries). Reminder rescheduling also coalesces the several store updates a sync cycle makes into one scan. (#766)
- Friendlier first run for people new to GTD: the welcome screen and the Getting Started project now use plain language, a new starter task shows how to hide task-editor fields you don't use, and an empty Focus view now explains how tasks get there (with capture guidance when the app has no tasks yet).
- The Getting Started project, its tutorial tasks, and the sample inbox items are now created in your app language (17 languages). Re-adding the content after switching languages repairs it in place instead of duplicating it. The first-run welcome screen is translated too.
- The AI assistant now works with newer Claude models (Sonnet 5, Opus 4.7/4.8): requests no longer send a temperature these models reject, and the thinking toggle uses their adaptive thinking mode. Enabling thinking on older Claude models no longer fails either. (#857)
- Android text no longer loses its last letter or line on Android 15/16 at large font sizes ("Off" showing as "Of", settings descriptions ending mid-sentence): the app now pins the pre-Android-15 text drawing behavior so drawn text always matches its measured space. Settings rows and the welcome screen also adapt their layout at large font scales instead of squeezing text into narrow columns. (#632)
- Reopening into Calendar, Archive, or another non-tab screen now keeps the main tabs in the navigation stack, so the iOS header Back button remains available and Android Back returns to the app instead of closing it. (#842)
- Desktop settings toggles no longer get squashed into ovals when the window is narrow — the switch now keeps its shape and the label text wraps instead. (#858)
- Desktop keyboard shortcuts no longer act on the task list behind an open dialog: with search, quick add, or a prompt open, a stray Enter or action key could complete or open a task in the background list (reported as an accidental Done from inside search). Keys pressed inside the search dialog outside its input now also keep driving the search — arrows and Enter navigate results, and typing returns to the query field.
- Updated bundled dependencies to pick up upstream security fixes (hono, undici, tar, vite, js-yaml, Babel on the JS side; plist/quick-xml, quinn-proto, anyhow in the desktop app).
- The "What's the next action?" prompt (shown after finishing a project's last action) now understands quick-add syntax, so `Chase the reply /waiting %Bob` creates the follow-up as a Waiting For task directly — no need to create it as a next action and edit it afterwards. (#859)
- The Focus toolbar now matches every other list: its buttons and the grouping dropdown share one height and shape, the grouping dropdown carries the same `GROUP` label as elsewhere, and its text is no longer clipped at the bottom. (#861)
- Focus now keeps future-start tasks hidden until their start day without showing a Show/Hide notice or control, on desktop and mobile. The separate reveal control in the Next list is unchanged.
- Desktop lists now set a task's status straight from the keyboard: press `s` then a letter — `si` Inbox, `sn` Next, `sw` Waiting, `ss` Someday, `sd` Done, `sa` Archived — with an undo toast after the change. The chord works in all three keybinding styles, and `Insert` jumps to the add-task input (or opens quick add). (#860)
- Desktop can now capture email into the Inbox (Settings → Integrations → Email capture): point it at an IMAP folder with an app password (Gmail, iCloud Mail, Fastmail, or any standard IMAP server) and, while Mindwtr is running, each email in that folder becomes an Inbox task — subject as the title, sender and body as the description. Mail is read-only: nothing is modified, moved, or deleted, the folder is created for you if missing, and already-imported messages are remembered so nothing imports twice. (#35)
- The desktop "switch to Area" chord is now forgiving: keeping Shift held from the `A` into the number (which types `!`, `@`, …) still switches areas, pressing Shift mid-chord no longer cancels it, and with Caps Lock on a plain `a` opens quick add instead of silently arming the chord. The shortcut help sheet now spells the chord as `Shift+A 1-9` so it reads as typed. (#865)
- Adding a task to a project during the weekly review is no longer title-only: the prompt understands the full quick-add syntax (`Chase quote @phone %Bob /waiting`), and a new "Save & edit" button creates the task and opens it in the editor — without leaving the review or losing your place. Works on desktop and mobile.
- The desktop Proxy URL setting now applies to all sync traffic. Self-hosted cloud, WebDAV, and Dropbox connection requests had been going out directly, ignoring the saved proxy (only reachable via `HTTP_PROXY`/`HTTPS_PROXY` env vars as a workaround); they now route through the configured proxy, taking effect on save with no restart. An invalid proxy URL is rejected when saving instead of being silently skipped. (#864)
- Completing a recurring chore no longer clutters Next actions with the occurrence you cannot start yet: a repeat that carries only a due date (a bimonthly "descale the kettle") now stays out of Next actions until it comes due, exactly as it already did in Focus. The deferred occurrences are counted by the "hidden (future start)" notice above the list, with a **Show** toggle to reveal them. The Android daily review defers them the same way. (#867)
- "Repeat after completion" tasks that also pick weekdays (or a day of the month) now honor the **Repeat every** interval. An "every 2 weeks on Sunday" chore completed on a Tuesday used to come back the very next Sunday — and the calendar preview showed the same too-early date — no matter what interval was set; it now returns on the Sunday of the second week after completion. Fixed-schedule repeats are unaffected. (#867)
- Desktop keyboard shortcuts no longer go dead after deleting a task from its open editor (for example Quick Add → Save & edit → Delete): the app was still treating the removed task as being edited and kept suppressing every shortcut until another edit was opened by mouse. (#870)
- The Trash now supports bulk actions on desktop and mobile, completing the set alongside Completed and Archived: tap **Select** to pick individual tasks and projects (or **Select all**), then restore them to the Inbox or delete them permanently in one step — permanent deletion still asks for confirmation. (#863)
- The desktop Add Task dialog is now fully keyboard-driven: `Ctrl+Enter` (`⌘Enter` on macOS) saves and opens the task in the editor, and `Shift+Enter` saves and keeps the dialog open for the next capture — batch entry without touching the mouse (the Save buttons show these shortcuts as tooltips). Tab no longer stops on the dialog backdrop or the `Esc` chip, and the syntax help line now sets the typable tokens (`/start:`, `@context`, `+Project`, …) apart from their `<placeholder>` parts in every language. (#869)
- The weekly review's final screen now ends with a short, calm status summary instead of only a congratulation: whether your Inbox is empty, whether every active project has a next action, and how many Waiting For items have sat untouched for more than two weeks. Each line mirrors what the matching review step showed, and the good/needs-attention rows read at a glance without scores or noise. Works on desktop and mobile. (#871)
- Setting a start date on an Inbox item now clarifies it to Next — the same shortcut starring already took. Deciding *when* you can act on something is a clarify decision, and before this an Inbox item with a start date was invisible in Focus (Focus only lists actionable tasks), which read as the date being ignored. Someday and Waiting items keep their status when dated (a dated someday is a tickler, a dated waiting-for a follow-up reminder), picking a status in the same edit wins, and a date arriving still never changes status on its own. Works everywhere tasks are edited or captured: editors, quick-add `/start:`, review defer, and the MCP/CLI/API surfaces.
- Desktop on older macOS (WebKit before Safari 16.4) no longer crashes to the error screen when showing a task whose description contains notes — opening such a task, or a Focus/Agenda list containing one, threw "Invalid regular expression: invalid group specifier name". The markdown email-autolink pattern now avoids the regex feature those systems lack, and if markdown rendering ever fails again the description falls back to plain text instead of taking the view down.
- The desktop quick-add suggestion popup now lists every slash command the parser understands: typing `/link`, `/energy`, `/area`, `/archived`, or `/*` (add to today's focus) no longer leaves you without suggestions. `/energy:` goes further and offers the three valid levels, so a stray `/energy:hi` can't end up as task title text. Accepted in the task editor's title field, `/energy:` and `/area:` fill the matching editor fields directly, and `/*` stars the task for today. (#869)
- Arrowing through the desktop suggestion popups now shows which row is selected on every theme. On Nord the highlight color was identical to the popup background, so arrow keys looked like they did nothing (Enter was in fact accepting the invisible selection); Nord's muted surfaces now use the palette's designated highlight shade, and the selected row uses the stronger accent style the search palette already had. On E-ink, where every surface is white, the selected row gets an outline instead. (#869)
- The small window the global quick-add shortcut opens no longer frames the capture card with a thick dark border: the card now fills the window edge to edge. (#869)
- Mobile screens you have navigated away from no longer re-render while you work. Opening a project from the Projects tab, global search, and a notification leaves three copies of that screen stacked, and every edit used to re-render all of them — about a second each on a 130-task project with a large library, so a single Done took over four seconds. Only the screen in front of you renders now; the others keep their scroll position and state and catch up when you come back. (#766)
- Today's Focus can now be arranged by hand, so the list reads in the order you plan to work it: on desktop, drag rows with the grip handle; on mobile, tap the reorder toggle on the section header to drag tasks and tap Done. Manual order applies while the Focus sort is at its default (an explicit sort still wins), it syncs across devices, and a task keeps its place until it leaves Focus. (#876)
- The mobile Pomodoro timer's Focus/Break phase label now follows the app theme instead of always showing blue/green — on Nord it no longer sticks out as an off-palette blue. (#875)
- The desktop Pomodoro panel is much more compact and can now be folded away: a chevron collapses it to a slim row showing just the time and phase (the timer keeps running), the fold state is remembered, and pressing a task's play button reopens the panel. Linking a task to the timer now uses a searchable picker instead of a long dropdown. (#875)
- Finishing a project's last action now offers to complete the project on the spot: the "What's the next action?" prompt gains a **Complete project** button (desktop and mobile) that archives the project — the same reversible action as the Archive button, completing any remaining tasks. The project details helper text now also says plainly that archiving is how a project is completed. (#874)
- Project lists now show the project's next action according to the order you arranged, not the order tasks were created: the task at the top of the project (after drag-reordering) is the one surfaced, on desktop and mobile. Projects without a manual order keep showing the oldest next action. (#873)
- The mobile quick-capture sheet now shows the full quick-add syntax reference (including `/note:<text>`, `/link:<url>`, and `/energy:<level>`) instead of only the short `@context +Project #tag` line.
- The desktop keyboard-shortcuts help (`?`) now ends with a quick-add syntax table, so every token the parser understands is one keypress away.
- Completing with the real finish time now works from every Done control: on mobile, long-press the swipe-revealed **Done** button (with a haptic cue), the **Done** status chip in the task editor, or the status badge in the task preview; on desktop, right-click a task's checkmark in the row or open editor, or the editor's **Done** status chip. Backdating then switching to another status before saving no longer leaves the picked time on the task. (#835)
- Desktop task date quick actions now respond to **Save** or **Cancel** with one click after using a quick-date shortcut. Picking a day from the mini calendar also stays in the draft until **Save**, instead of updating the task immediately. (#878)
- The desktop Review page top area is now a single row: search, the status filter, and the list controls sit together above the tasks, with sort, grouping, and the details toggle gathered into one **View** popover (no more duplicate Details button or a second half-empty toolbar row). On narrower windows the status chips collapse into a compact Status dropdown.
- Desktop toolbar dropdowns — Sort, Group, and the Review status filter — now open a themed menu that matches the app instead of the operating system's bare native list, which looked broken inside the Review View popover. Keyboard behavior matches a native select: arrows move (skipping disabled entries), Enter/Space picks, Escape closes without changing anything.
- Tasks written through the cloud REST API (including MCP assistants in cloud mode) now follow the same rules as the apps: setting a start date on an Inbox task clarifies it to Next, starring an Inbox task promotes it, demoting a starred task to Inbox removes the star, and stale board/Focus positions are cleared when status changes — previously these API writes skipped the apps' shared update rules, so a dated Inbox task stayed invisible in Focus. Board column position can now also be updated through the API, like the other ordering fields.
- A task that leaves Today's Focus by being completed or archived now releases its manual Focus position, so starring it again later adds it at the end instead of jumping into its old slot.
- Reordering Today's Focus by hand is paused while a search or filter narrows the list (desktop drag handles and the mobile reorder toggle both hide), because rearranging a partial view would scramble the positions of the focused tasks that are filtered out. Clear the filter to reorder the full list.
- The desktop task editor now follows the field order set in Settings → GTD → Task Editor Layout for the Basic section: Status (and any other basic field) can sit above or below the Area/Project/Section row, and the selectors inside that row follow the configured order too — previously that row was always pinned to the top. (#880)
- Input placeholders on desktop are now clearly dimmer than typed text in every theme, and the Area/Project/Section selectors show "No area" / "No project" in muted text when nothing is picked, so example hints like `@home, @work` no longer look like values you entered. (#881)
- Fixed an Android crash when calendar sync created an event on devices whose calendar app returns an unexpected event ID: the app now reports the event as not saved and retries on the next sync instead of crashing.
- Fixed the 1.1.5-rc.1 Android storage regression: the new native SQLite engine failed to initialize because multi-statement schema scripts were split apart mid-trigger, so the app silently ran on its JSON backup — and on large libraries (roughly 2 MB of data or more) that backup could not be read back, leaving the app blank with sync failing. SQLite now initializes correctly, and any tasks or edits made while on rc.1 are merged back into the database on first launch after updating. (#766)
- Keyboard navigation from the sidebar into a task list (`ArrowRight` or `l`) now selects and focuses the current task instead of drawing a focus outline around the entire list, and in Focus the first `ArrowDown` after entering moves exactly one row instead of skipping to the second task. (#890)
- Tasks with a timed due date now offer a "Skip reminders" toggle in the task editor on desktop (the existing mobile switch was renamed to match): the task stays visible in Focus and lists but never fires start/due reminders — useful for recurring chores you track without wanting notifications. (#885)
- Tapping the voice button with no speech-to-text model configured no longer silently closes the add-task dialog: it now stays open and shows a notice pointing to Settings, on desktop and mobile. Note: this replaces the old fallback of saving an untranscribed audio attachment when speech was never set up. (#886)
- The iOS Add task sheet scrolls when the More options are expanded, so the title box and the extra fields are both always reachable. (#887)
- Saving a Focus filter on iOS no longer freezes the app: the save-name prompt used to open as a second overlay on top of the filter sheet, which iOS could silently refuse — the Save button looked dead, the filter was never stored, and navigating away could hang the app. The prompt now appears inside the same sheet and saving works. (#889)
- Quick add can now leave plain-language dates alone: a new "Detect natural-language dates" toggle (Settings → GTD, on by default) controls whether bare phrases like "next week" in a title become dates. Turned off, "Register for the race next week" stays literal text with no due date, while explicit tokens (`/due:next week`, `@context`, `#tag`, …) keep working and being cleaned up exactly as before. The setting syncs across devices. (#742)
- Filter chips can now exclude: tapping a context or tag chip in the Focus/list filters cycles through off → included → excluded (shown struck through in red), so "everything except `#someday-maybe`" is one tap — and mixes freely with included chips ("show `@errands`, hide `#waiting`"). Excluded chips always subtract regardless of the Any / All setting, saved Focus filters remember them, and the state is announced to screen readers on desktop and mobile. (#893)
- Tag filters gain the same Any / All switch contexts got: with two or more tags selected, a "Tag match" control appears next to "Context match", so `#quick` and `#calls` can mean "either" instead of "both". `All` stays the default, and saved Focus filters remember the choice. The "Context match" and "Any" labels are now also properly translated in all languages instead of always showing English. (#892)
- The iOS Add task sheet's picker overlays (Contexts, Project, Area, Priority) no longer hide their bottom rows behind the keyboard: the picker card now lifts above the keyboard the way it already did on Android, and follows the keyboard when the predictive-text bar appears or disappears. (#891)
- Settings are findable by what they're called: the desktop settings search now indexes the actual (translated) titles of every setting on each page, so typing `default`, `project`, or `area` surfaces "Default project flow" and "Default area for new tasks" instead of nothing; mobile Settings gains the same search box at the top of the list; and the time-estimate presets editor arrives on desktop (Settings → GTD), matching the one mobile already had. (#884)
- The mobile Pomodoro end-of-phase alert no longer goes missing while the app is in the background: opening the Focus tab could briefly cancel the pending alert while the saved session was still loading, a re-schedule could be interrupted between removing the old alert and creating its replacement, and on iOS any two notifications created within the same second silently replaced each other (which could also swallow task reminders scheduled in a burst). All three paths are fixed. (#888)
- The mobile app now keeps the self-hosted sync token and WebDAV password in the platform keystore (iOS Keychain / Android Keystore) instead of plain app storage, where they could end up inside device backups. Existing credentials move over automatically the first time the app reads them, and the plaintext copy is scrubbed. Dropbox tokens and AI keys already lived in the keystore; this closes the gap for the remaining two secrets. Reported privately by Alicia Sykes (GHSA-8x25-76x5-jgmr).
- When a Pomodoro phase ends while the desktop window is in the background, Mindwtr now also flashes its taskbar entry (Windows and most Linux desktops; on macOS the Dock icon bounces) and keeps it highlighted until you switch to the window — a cue that survives a buried or missed notification, and works where system notifications can't show at all, such as the portable Windows build on Windows 10. (#528)
- The MCP server (`mindwtr-mcp`) gains an opt-in HTTP mode for remote MCP clients: start it with `--http --http-token <secret>` to serve the same tools at a URL (default `127.0.0.1:8722`, endpoint `POST /mcp`) instead of stdio — this is what lets a self-hosted Mindwtr connect to Gemini's custom apps, which take an MCP server URL. A bearer token of at least 16 characters is mandatory (the server refuses to start without one), works with both the local database and cloud mode, and stdio remains the default, unchanged. (#465)
- The mobile Inbox and Focus screens are calmer. The Process Inbox button is now an outlined tonal button with breathing room below the toolbar instead of a solid blue bar competing with the capture button, and Mind Sweep sits as a neutral control beside sort and filter. Focus section headers drop their horizontal rules and blue all-caps color for quiet gray labels. Task cards everywhere are slimmer: tighter vertical padding, and a checklist's progress shows as a small inline count next to the other metadata instead of a full-width bar.
- The Archive page can now show archived projects, not just tasks: a Tasks | Projects switch at the top filters the list (never mixing the two), with search working in both views. Archived projects can be restored — which also brings back the tasks that were auto-completed when the project was archived — or deleted to the Trash after a confirmation, on desktop and mobile.
- The mobile Done list now shows a proper log by default: most recently completed tasks first, matching desktop. It previously fell back to the generic list order (due date, then creation time), which read as random for finished tasks. (#896)
- The swipe actions on the mobile Archive and Trash screens ("Restore", "Delete") now show in the app language instead of always English.
- The desktop WebDAV "Test Connection" button now works with saved credentials after an app restart: it used to send an empty password (reporting 401 Unauthorized) unless you retyped it, even though sync itself kept working with the stored password. The test now uses the same saved credentials sync uses, and a failed test is recorded in the shared log. (#899)
- WebDAV sync errors now say exactly what failed: the error message and the shared log record the request method, the final file URL, the HTTP status, and the server's response — instead of a bare status code and the configured folder path. A remote read that unexpectedly finds no data is also logged with the URL it hit. This makes server-specific rejections (like Koofr's intermittent 405s) diagnosable from a single shared log. (#898)
- Large libraries on mobile no longer stall for 10–20 seconds after saves: the full-library JSON safety copy (kept for downgrades and recovery) now lands at most once every 5 minutes while you work — and immediately when the app goes to the background — instead of after every save, since the SQLite database is the durable copy. Home-screen and lock-screen widgets keep refreshing right away. A sync that only stamped its status timestamp also no longer re-renders settings-dependent screens or re-arms reminder scheduling, and shared logs now break down backup size, per-step sync timing, and the post-sync refresh so remaining slowness can be pinpointed exactly. (#766)
- Desktop task lists no longer draw a second, input-looking outline around the selected task's title: the row highlight is the single selection marker, and arrow-key navigation moves it cleanly from row to row without leaving a stale highlight behind. The focus outline inside the task editor (inline and popup) is also no longer cut off at the top and left edges. (#860)
- The silent "Add to Mindwtr Inbox" iOS Shortcut now understands quick-add syntax: text like `Buy milk /due:friday @errands #personal +Groceries` is parsed exactly like the in-app capture box (dates, contexts, tags, projects, and the other tokens), honoring the "Clean up quick add text" setting instead of landing verbatim as the task title. The Shortcut's separate Note/Tags/Project fields keep their meaning and win over tokens typed in the text, and a capture that can't be parsed still lands in the Inbox as-is. (#895)
- The desktop Next actions list no longer offers a "N hidden (future start) · Show" reveal: future-start tasks now stay hidden everywhere until their start day, matching Focus on both platforms. The old show/hide setting from earlier versions is ignored, so a synced "Show" preference from an older device can't bring the reveal back. Future-start tasks remain visible in Projects and Search when you want to look ahead. (#900)
- The desktop Completed, Waiting, and Someday lists now have a **Filters** button in their toolbar, so you can narrow the list by context, tag, priority, or time estimate before acting on it — for example, filter Completed down to one context, then Select and bulk-archive the matches. Mobile already had this; the panel and any active filters are shared across the three lists and the summary chip under the title shows what's applied. (#863)
- The calendar's "Projected" preview of a repeat-after-completion task no longer lands on the same day as the task's real next occurrence (an every-3-days task due the 21st showed a duplicate ghost on the 21st): the preview now shows the occurrence after it — the 24th in that example — assuming the task is completed on time. Overdue repeats still project from today, and date-only tasks no longer pick up a stray time in the preview. (#900)
- With iCloud sync, a Focus star set on iPhone/iPad now reaches the Mac. The Mac was reading on/off values from iCloud in a form the sync merge didn't recognize, so it silently turned them off while keeping the rest of the task — the same edit could sync a title change but drop the star. This also affected other on/off values coming from iOS: reminder muting, the calendar's future-occurrence preview toggle, a project's sequential setting, and project Focus. Values set on the Mac always synced to iOS correctly. Tasks that already lost a star this way need it toggled once more on the phone after both apps update. (#902)
- Clicking Save or Cancel right after picking a date in the desktop task editor no longer misses: the quick-date row (Today, Tomorrow, +3 days…) used to collapse at the instant of the click, shrinking the dialog and pulling the button out from under the cursor. The row now stays put until the click lands, then tidies up. On Android, the same quick-date row for the due date is now visible without opening the system date dialog — matching the start and review rows — so one tap sets Tomorrow without touching the picker. (#901)
- Mobile sync no longer claims "No internet connection" when the device is online but Mindwtr's own requests to the sync server keep failing (for example cellular data turned off for the app, or a VPN, DNS filter, or firewall blocking it). The toast now says the sync server couldn't be reached and points at the app's network access; a genuinely offline device keeps the offline message.
- Mobile task lists now show the Time Spent badge next to the other row metadata, matching desktop: with the Pomodoro timer and task linking enabled, a task's recorded time appears in the row instead of only inside the task editor.
- The mobile task editor's top-right button now says **Save** instead of **Done** — it commits your edits, and "Done" read as if it referred to the task status shown right below it.
- Typing a checklist on mobile no longer needs a tap of "+ Add item" between entries: pressing return on a filled item inserts the next empty item right after it and keeps the keyboard up, matching the desktop editor's Enter. Pressing return on an empty item ends editing, and "+ Add item" now puts the cursor straight into the new row.
- Android daily digest, weekly review, and repeating task reminders no longer drift away from their set time. Android treats repeating alarms as inexact and defers them under battery optimization (hours late on strict systems like GrapheneOS, and worse after reboots); each occurrence is now scheduled as an exact alarm that re-arms the next one when it fires, the same mechanism one-time task reminders already used. Times also stay on the wall clock across DST changes and reboots. (#905)
- Switching areas on desktop is now a single keystroke: pressing `1`-`9` (outside any text field) jumps to that area by sidebar order, and `0` clears the area filter — faster than the `Shift+A` chord, which keeps working unchanged. Digits with no matching area do nothing. (#865)
- The German status tag on active projects now reads "Aktiv" (a Turkish "Aktif" had slipped into the German translation). (#904)
- Each project now remembers its own task sort order: pick a sort inside a project (due date for a deadline-driven project, manual order for a packing list) and it stays that way across restarts and syncs to your other devices. Desktop's project sort also catches up with mobile's options — the old Default/Due pair becomes the full sort dropdown (start date, review date, title, oldest, newest). Manual drag-reordering still applies while a project is on Default. (#903)
- Desktop task lists gain a third density, **Condensed**, with rows about 20% shorter than Compact so large lists fit more tasks on screen. Pick it under Settings → Appearance, or use the toolbar Density button (and the toggle-density shortcut), which now cycles Comfortable → Compact → Condensed. (#906)
- Batch capture works on iOS: pasting multi-line text into the quick-add box now offers to create one task per line (matching desktop) instead of one long task, and the quick-add "Import .txt" button no longer fails silently — its "create N tasks?" confirmation used to be swallowed by the closing file picker, so nothing appeared to happen.
- Self-hosted sync tokens shorter than 20 characters no longer create a server that rejects everyone: the cloud server used to load such a token into its allowlist, log success, and then 401 every request (the request path enforces 20-512 characters). The server now refuses to start with a clear message when a configured token can never authenticate, and the desktop and mobile settings forms reject an invalid token when you enter it instead of saving it silently.
- Manual sync no longer reports "Sync completed" while the sync indicator shows a failure: when the upload to the sync server fails and is queued for background retry, the Sync now button on desktop and mobile reports the actual error instead of a green success. Self-hosted setups pointing at the wrong address also get a clearer message — a 405 response or an HTML page now says to check the Self-Hosted URL, host, and port instead of surfacing a raw JSON parser error.
- The desktop task editor's date suggestions now live inside the calendar popover, in a column beside the month grid, instead of a row that appeared under the date field. Focusing a start, due, or review date no longer shifts the editor layout, the shortcuts open with the calendar button, and a new **+2 days** option joins Today, Tomorrow, +3 days, Next week, Next month, and No date. (#901)
- Sequential projects are easier to read at a glance on desktop: tasks waiting for an earlier action now render slightly muted in the project view, so one glance shows the single live next action against the queued rest. Hovering a muted task restores it to full strength, and its tooltip says "Later in sequence". Parallel projects look unchanged.
- Tasks added to today's focus are now visible at a glance across desktop lists and the Board view: a focused Next task shows a small filled star beside its title. The star is a pure indicator; adding or removing focus stays in the task's quick-actions menu (and on the Focus page). (#908)
- The "Detect natural-language dates" toggle now truly syncs across devices: the receiving side always understood the setting, but the sending side never included it in the sync payload, so a change made on one device stayed on that device. (#742)
- Windows now applies the minimal WebView2 phone-home switches through WebView2's environment append path instead of Tauri's per-window `additionalBrowserArgs` config. The config path was the only Windows runtime change shared by rc.3 and rc.4, where it could leave the browser-to-native channel stuck (saves never returned, sync waited forever, and closing could not reach the native quit command); the append path is the one the reporter verified working across the same flag combinations. Component updates, domain-reliability reporting, and hyperlink-audit pings remain disabled without replacing Tauri's own WebView2 defaults. (#909, #913)
- Bulk actions on mobile behave the same everywhere: multi-select delete on the Review, Contexts, and Archived screens now asks for confirmation and offers the same undo toast as Inbox, Waiting, and Someday (Review previously deleted immediately with no confirmation or undo). Bulk actions also report a real error instead of a false "done" toast when the underlying change could not be applied (for example when another device removed one of the selected tasks moments earlier).
- Grouping by area on desktop now labels tasks without an area "No Area" (matching mobile and the task editor) instead of the ambiguous "General". This applies to every desktop view that groups by area: Next, Reference, Contexts, and Today's Focus.
- Grouping Today's Focus by person on mobile now lists the "Unassigned" group last, after every named person, matching desktop (it previously sat alphabetically among the names).
- Grouping Today's Focus on mobile now colors each group's header dot the way desktop does: project and area groups show their entity's color, and context/tag groups get their stable token color (previously every mobile group dot used the generic accent color).
- Completing a recurring task moments after opening the app no longer risks the completion briefly reverting on screen and the next occurrence never being created. A completion that landed while the app-open sync was still reading local data could be silently overwritten by that sync's stale snapshot; the sync now detects the mid-read change and requeues instead. (#910)
- Mobile task cards now use a flat, outlined style — the clean look reorder mode already had — instead of a drop shadow, on task lists and the Archive and Trash screens. Dark mode is unchanged (it never had shadows). (#912)
- In sequential projects sequenced per section, completing a section's last next action now asks "What's the next action?" for that section — matching the prompt that already appeared when a whole project ran out of next actions. The prompt lists only that section's tasks, a task added from it lands in the section, and since the rest of the project is still live it doesn't offer Complete project. Desktop and mobile. (#911)
- On phones and tablets, the home-screen widget no longer re-renders after every change while you are inside the app — a multi-second cost on large libraries that made completing tasks feel slow (#766). The widget now refreshes at most every five minutes while the app is open and updates immediately the moment you switch away, so what you see on the home screen stays current.
- The macOS desktop app now declares Local Network usage, so macOS 15+ shows the permission prompt on first contact with a LAN address instead of silently blocking it. Connecting to a self-hosted sync server on a local IP (for example behind a home reverse proxy) previously failed on the Mac while the iPhone app, which already carried the declaration, prompted and worked.
- Typing a task with a trailing date into quick capture now sets the due date even with "Clean up quick add text" off (the default): "do this jun 26 10am" used to save exactly as typed with no date, because date detection was tied to the text clean-up setting instead of the "Detect natural-language dates" toggle that documents it. The title still keeps your text exactly as typed; turning clean-up on additionally removes the matched date words from the title. Applies to desktop and mobile capture, list inputs, and prompts.
- Adding a task to a visible list now shows you where it landed: the new row scrolls into view with a brief accent flash, on desktop lists and project views (including tasks added through the project's quick-add dialog) and in mobile project lists fed from quick capture. Focus stays in the input, so batch entry keeps flowing; sorted lists no longer swallow a new task somewhere off-screen. (#916)
- The Android home-screen widget now shows when a task is due, at a glance: dated rows get a short right-aligned label ("Today", a weekday for the coming week, or a compact date), highlighted when a task is overdue or due today. Rows without a due date and the narrow widget layout are unchanged, so the widget stays as calm as before.
- Quoted names in quick add now survive smart-quote keyboards: `%"Jim Smith"` typed on a phone whose keyboard curls the quotes (or mixes a curly opening with a straight closing quote, German „ quotes, or single quotes) used to fall apart into an assignee named `"Jim` with `Smith"` left in the title. All common quote styles now delimit the name correctly, and the same tolerance applies to quoted `+"Project"` and `!"Area"` names. (#849)
- Quick add now understands European dot dates: a trailing "26.06." or a full "26.06.2026" at the end of a task sets the due date day-first, with times ("26.06. 18:00") and the explicit "/due:26.06." form working too. The trailing dot or year is required on purpose, so version numbers like "python 3.12" and decimals never turn into dates.
- The Standard keybinding preset's task-action keys now work wherever focus sits inside a task row: `Shift+Enter` opens the edit view even when a row control (done check, title, star, options, or a context chip) holds focus instead of activating that control, and `x` toggles multi-select in every view — Projects, Contexts, Search, and Review previously ignored it because those lists lacked the select action Inbox-style lists had. (#847)
- Two more desktop list shortcuts: `S` (capital) adds the selected task to today's focus or removes it, and `F2` puts the cursor straight into the selected task's title for renaming — both work from anywhere in the row and appear in the `?` shortcut help. The lowercase `s` status chord is unaffected. (#847)
- The desktop task editor now shows each field's quick-add token as a quiet inline badge — `@context` on Contexts, `+Project` on Project, `/due:` on the due date, and so on. Hovering a field badge keeps the fuller "Quick add" explanation. (#918)
- MCP task writes no longer report a false failure when the change actually saved: with stale search-index content, completing or updating a task through `mindwtr-mcp` used to return an internal error (inviting a retry and duplicate actions) even though the change had durably persisted. The response now returns the task with a storage-health warning instead, and still fails plainly when the write itself could not be confirmed — creates, deletes, and non-task changes keep failing loudly, since they have no confirmed partial save. The next launch repairs the derived search indexes once, and trigger maintenance is now atomic so a running desktop app cannot recreate the mismatch; corruption errors still point at SQLite's `PRAGMA integrity_check`, and the MCP server tolerates brief lock contention instead of failing instantly. (#922)
- Editing a task on Android no longer hides the description behind the keyboard: tapping the description (or any lower field) now scrolls it above the keyboard reliably — the first tap after opening the editor previously never scrolled, and reopening a task with existing notes could leave only the field's label visible. Scrolling the form while the keyboard is open no longer dismisses it, so you can glance back at what you wrote without losing your place. (#921)
- The "Allow insecure connections (HTTP)" sync setting now does what it says: with it enabled, a self-hosted or WebDAV server reachable only by a private hostname — a NAS behind custom DNS, a Tailscale or ZeroTier name, a VPN host — is accepted over HTTP instead of being rejected as a "public HTTP sync URL". The toggle had no effect (local IPs and `.local` names were always allowed, everything else always blocked), which stranded servers the app cannot recognize as private. With the toggle off, nothing changes: HTTPS is required for anything beyond localhost, private IP ranges, and local hostnames. (#920)
- The AI assistant's model lists are current again across all three providers, and requests survive provider-side model retirements: Google retired the app's previous Gemini default (gemini-2.5-flash) server-side in July, which broke voice dictation and assistant calls with a blank "AI request failed" dialog. Defaults now point at the current tiers (Gemini 3.6 Flash, Claude Sonnet 5, with Claude Haiku 4.5 and GPT-5.4 tiers unchanged), suggestion lists drop retired ids like claude-3-5-haiku, and a saved retired model in your settings is transparently upgraded to the closest current tier at request time — custom or self-hosted endpoints keep their model names untouched. AI failure dialogs on mobile now also show the provider's actual error text, so the next model retirement diagnoses itself.
- The desktop project list now matches mobile: the "No next action" warning is reserved for focused active projects, and an Inbox task is no longer presented as the project's next action. Weekly Review still checks every active project. (#927)
- Completing a task through the completion-time prompt can now log time spent alongside it, on desktop and mobile: with the Pomodoro timer and task linking enabled, the prompt gains a Time Spent field seeded from the task's already-tracked total — a correction alongside automatic focus-session tracking, not a replacement for it. (#896)
- If desktop Mindwtr cannot finish saving before closing (a stuck save, or the hung native channel behind #913), the window now stays visible and comes to the front instead of silently hiding into a zombie process that still holds your unsaved edits. After about 10 seconds you're asked whether to close anyway (unsaved changes will be lost) or keep waiting, and closing again (the X button, Alt+F4) is no longer silently ignored while a save is in flight. A save that has not completed after 15 seconds also now shows a warning so a stuck save is never silent. (#913)
- Desktop quitting (from the close prompt, or a "quit on close" setting) now always waits for that same pre-close save to finish first, whether it's already in progress or hasn't started yet — closing the window and quitting used to be independent steps with nothing guaranteeing the save came first. The 15-second stuck-save warning also now covers individual task saves, not just full data saves, since those are the more frequent save on every task edit. (#913)
- Re-importing the same DGT GTD export no longer creates a second copy of everything. DGT imports now derive stable ids from the export's own entries, the way TickTick and Todoist imports already do, so importing the same file twice updates what is already there instead of duplicating every area, project, and task.
- Filtering works the same way on Focus and the task list now: both use one filter sheet, so a `15min` time estimate is offered on Focus where it was missing, and the task list offers the time-estimate presets you configured in settings instead of a fixed list. Chips and their tap targets grew to a comfortable 44pt on the task-list sheet, and long tokens wrap instead of overflowing at large system font sizes.
- Keyboard navigation in the desktop calendar now skips projected repeats. `j` and `k` used to stop on a projected occurrence of a recurring task — a display-only preview that is not a real task — where completing or setting a status silently did nothing and delete called through with an id no task owns. Moving between chips also no longer opens a task's editor as a side effect of arriving there.
- Offline voice dictation on mobile stops losing a model it already downloaded. If the Whisper model's path contained an encoded character, the app checked one form of the path when downloading and a different one when reading, so a model that was on disk could report itself missing — the download and the speech engine disagreed about where it lived. Both now resolve the path the same way, and the AI settings screen no longer flashes "not downloaded" for a moment before recognising an installed model.
- A project's review date no longer shows a phantom midnight time. A date-only review date rendered as "…T00:00" in the project workspace while the same date displayed correctly on tasks, because the two screens formatted dates through different helpers. They now share one, and a date-only value stays date-only everywhere.
- The desktop calendar's day and week views no longer shrink unrelated events. A single pair of overlapping blocks anywhere in a day used to squeeze every block that day to half width (three overlapping made everything a third), including meetings hours apart with nothing between them. Side-by-side splitting now applies only to blocks that actually overlap each other.
- Global search now behaves the same on desktop and mobile. Searching `id:` for a completed task finds it on mobile as it already did on desktop; a task whose area was deleted now appears under "No area" on both; and full-text results are merged with the fallback search rather than replacing it, so a match that full-text indexing misses is no longer dropped.
- Android with a very large library is much quicker to respond again. Every task edit was rebuilding, sanitizing and hashing a copy of the entire library on the spot just to decide whether a sync was worth scheduling, which on a 5,000-task library cost about five seconds of frozen app per tap. That check now compares only what actually identifies a change, so completing or editing a task no longer waits on the whole library. (#766)
- The safety copy Mindwtr keeps outside its database is no longer written once a library grows past what Android can read back (about 2MB): the copy was costing several seconds per write while being impossible to reload, and a momentary database timeout would then strand the app on it — blank reads and a "Row too big to fit into CursorWindow" sync error until the app was restarted. Past that size the database is used directly and a slow read simply retries; if the database is genuinely unavailable, a save now reports the failure instead of appearing to succeed. (#766)
- Windows keeps the WebView2 defaults it is supposed to have. The privacy switches added in this cycle are passed through the environment, and if that value replaces rather than extends what Tauri normally sends, the app would silently lose the suppressed WebView2 mini-menu, SmartScreen (itself a source of the phone-home traffic those switches removed), and gesture-free audio playback for the Pomodoro alarm. Those defaults are now carried explicitly, and a `WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS` value you set yourself is merged with them instead of either side being dropped. (#913)
- Android no longer quits out of the blue during long sessions. Background sync and context automation run in a short-lived background context that Android tears down the moment their work reports finished, but pending database writes, safety-copy backups and widget refreshes were still running at that point — landing in memory that had already been released and taking the whole app down with it, with no error and no crash dialog, typically after a few hours of use. That deferred work now finishes before the background context goes away. Background sync also no longer runs several times at once: Android can deliver a batch of queued sync events together (three arriving in the same millisecond on one device), and those overlapping runs raced each other's view of your data.
- Long translated view titles are no longer cut off or split apart on desktop lists. In languages whose words run longer than the English ones — Portuguese "Aguardando" for Waiting, "Algum dia/Talvez" for Someday — the list toolbar claimed the space it wanted and squeezed the title until it was clipped with an ellipsis, and then, once titles were allowed to wrap, broken mid-word across two lines ("Aguardand/o"). The title now keeps room for its longest word and the toolbar wraps instead, in every list view and every language. (#923)
- Opening a task from search or an internal link now lands somewhere you can actually see it. A repeating task that is deferred until its next due date is hidden from Next, but clicking its search result still navigated to Next, so the task looked unreachable; those now open in Review, where deferred tasks are listed. Tasks with an ordinary future due date are unaffected, since they were never hidden in the first place. (#867)
- Completing a repeating task and then immediately completing the occurrence it just created no longer ends the series. With "repeat after completion" both completions fall on the same day and therefore produce the same next date, and the second occurrence was mistaken for a duplicate of the first and silently dropped — leaving the chore finished with nothing scheduled after it. This was easiest to hit from Review, which puts the freshly created occurrence right in front of you, but it applied anywhere two generations were completed on one day, including bulk actions. (#867)
- Starting the Pomodoro timer on a task outside Focus now sticks. The Play button on a task row is offered wherever a task appears, but the timer resolved its linked task against the Focus list alone, so linking anything not currently in Focus — a task opened from Review or the calendar — was dropped the moment the timer panel drew itself, leaving the button looking dead and no time recorded against the task. The link is now kept for any task that still exists, and the timer's task picker shows it so you can unlink it; a task that is deleted still releases the timer as before. (#867)
- Dragging a task onto the calendar is no longer a secret. In lists without manual ordering — Someday, Waiting, Reference, Review, Contexts — a whole task row is a drag source for the calendar, but nothing indicated where it could be dropped, so the grab cursor just read as a promise of reordering that those lists do not offer. The **Calendar** item in the sidebar now lights up as soon as you pick a task up, and highlights more strongly as you drag over it; dropping there (or resting on it for a moment) opens the calendar with the task still in hand, ready to drop on a slot. (#867)
- Clicking a task in the calendar no longer opens its editor jammed against the top of the window. The dialog was pinned to the very top edge, overlapping the page heading while the space below the calendar went unused. It now sits centred in the window, and an editor taller than the window scrolls rather than having its top cut off. (#867)
- A task can now be dragged onto a sidebar list to reclassify it. Pick a task up anywhere and drop it on Inbox, Someday/Maybe, Waiting For, Reference, Done or Archived, and it moves there with an undo option in the toast; every valid destination lights up as soon as you start dragging, so the targets are visible rather than guessed. Trash is deliberately not a drop target, so a stray drag can never delete anything, and dropping on Calendar still opens the calendar with the task in hand as before. (#867)
- Dragging a task onto the calendar now lands somewhere you can actually drop it. The calendar reopens wherever you left it, and it cannot be navigated while you are holding a task, so whatever is on screen when you arrive is all you can reach: arriving in the Schedule view left you with no drop target at all, and arriving on a week you had browsed months ago meant a drop scheduled the task months away without saying so. A dragged task now always arrives on today, and the Schedule view hands over to the week view, which is the only one offering both a day and a time. Day, week and month views are otherwise left exactly as you had them, and ordinary navigation into the calendar is unchanged. (#867)
- The sidebar now actually shows where a dragged task can go. Picking a task up was meant to mark every valid destination, but the cue never appeared: only the entry directly under the pointer reacted, and only faintly. Every destination — Inbox, Someday/Maybe, Waiting For, Reference, Done, Archived and Calendar — now takes a dashed outline for as long as you hold the task, and the one under the pointer fills in solid, so the target you are about to drop on is unmistakable. (#867)
- Right-clicking a task on the desktop calendar now opens the same quick menu that right-clicking a task in a list does — status, start date, due date, review date, area, contexts, duplicate and delete — plus a new **Remove from calendar** entry that takes the task off the calendar by clearing whichever date put it there, with an undo option. Dismissing any of these menus by clicking elsewhere now only closes the menu: that click used to fall through and act on whatever was underneath, which on the calendar meant closing the menu also opened the new-task composer. (#867)
- Dragging a task did nothing at all on Windows: the drag started and then every destination refused it. Tauri's own file-drop handler was intercepting drags before the app could see them, and it is now switched off, so dragging tasks onto the calendar or a sidebar list works there like it does elsewhere. (#867)
- Files can now be attached by dragging them straight onto an open task editor, on top of the existing **Add file** button. Dropped files are copied into Mindwtr's storage the same way picked ones are, and the editor opens the section holding Attachments and scrolls to it so you can see what landed and remove it before saving. (#867)
- Cancelling a task edit no longer leaves attachment files behind. **Add file** — and now dropping a file — copies the file into Mindwtr's storage straight away, but the attachment itself only exists once you save, so cancelling used to discard the attachment and keep the copy on disk forever unless sync happened to clean it up. Those copies are now removed when you cancel. Files a task already had, and linked files that live outside Mindwtr's storage, are never touched. (#867)
- Speech to Text can now use a self-hosted transcription server instead of OpenAI's. Set a base URL under Settings → AI Assistant → Speech to Text and recordings are transcribed there — any OpenAI-compatible server works (whisper.cpp, Speaches, LocalAI, vLLM, Groq), an API key is optional since most self-hosted servers do not use one, and the model name is now typed rather than picked from a fixed list so your server's own model ids work. Desktop and mobile. (#930)
- Weekday headers in the calendar and the date picker no longer overflow their columns. They come from your system's own locale data, and in some languages what that data calls the "abbreviated" weekday is far from short — European Portuguese returns the whole word ("segunda", "terça"), which overlapped and clipped, and Arabic, Polish, Vietnamese and Hindi were crowded the same way while French and Brazilian Portuguese carried trailing periods. Headers are now trimmed to three characters, or to the single-letter forms in languages where trimming would make two days look identical. English, German, Spanish, Italian, Turkish, Dutch, Czech, Russian, Japanese, Korean and Chinese are unchanged. (#929)
- Mindwtr now starts straight into the system tray when your computer launches it at login, with the tray icon enabled — no setting to find or turn on. Opening it yourself always shows the window as before; only the silent startup launch is hidden, and it needs the tray icon to bring the window back through. Mindwtr records the migration before replacing an existing startup entry; if registration fails, the next launch retries it instead of leaving launch-at-login off. Flatpak's Background portal now starts `mindwtr --startup`, so its login launch hides in the tray. Microsoft Store installs start through a Windows-declared startup task that cannot carry this signal, so the window shows there as normal. (#928)
- The web version now keeps the screen you are on in the address bar, so refreshing stays put — including inside Settings, which used to send you back to where you were before it — and a link to a view opens that view. It is a `?view=` parameter rather than a path, so it works on any static host without extra server configuration. Filters are still shared through saved filters rather than the URL. (#931)
- File attachments now upload to a self-hosted cloud server whose URL was entered without the `/v1` part. Tasks and projects synced fine, but attachments were sent one path level too high, the server answered 404, and the attachment stayed local with no cloud key — every later sync then failed with "Attachment upload incomplete". Attachments that got stuck this way upload on the next sync, with no cleanup needed. (#781)
- Korean is now a fully translated interface and is selected automatically when your system language is Korean. A contributor rewrote all 2,126 strings, replacing a machine translation that had turned product names into unrelated words — the Gemini AI provider read as the constellation, the Vim keyboard preset as an unrelated noun, and the Material 3 theme as "raw material 3". (#934)
- App icons no longer draw a thin white outline on dark backgrounds. The logo had been cut out from a white background, which left a pale rim baked into its edge pixels — visible around the mark in the desktop app under a dark or Nord theme, and anywhere the icon sits on a dark surface. The Android round launcher icons and the tray icon were also masked without anti-aliasing and had visibly stepped edges; they are now smooth. The small logo beside the app name in the desktop sidebar was a separate copy that had no transparency at all — its white background showed as a bright ring around the rounded mark — and it is now cut from the same clean artwork and rendered at a size that stays sharp on high-DPI screens. The artwork itself is unchanged. (#937)
- The sidebar sync footer no longer flickers while a sync is running. When a sync cycle queued a follow-up, the status briefly reported "not syncing" between the two runs, so the dot stopped pulsing, the Sync now button re-enabled — swapping its cursor and hover highlight under the pointer — and the spinner restarted from the top, which looked like the icon jumping around when you hovered it. Queued work now counts as syncing, so the footer stays steady until the whole cycle finishes. (#913)
- The desktop window now reopens at the size and position you left it, instead of resetting to the default 1200x800 on every restart. Maximized and fullscreen states are remembered too. A saved position on a monitor that is no longer attached falls back to the system default rather than opening the window off-screen, and portable installs keep the saved geometry inside their own profile folder. (#936)
- The desktop completion-time dialog's Time Spent field now behaves like the one in the task editor: it is a proper number input, so Up/Down arrows and the spinner adjust it, and Enter confirms the dialog from that field instead of only from the date input. Entering a fractional value now rounds to the nearest minute rather than dropping the decimal point. (#896)
- Clicking anywhere in a desktop date or time field now opens its picker, instead of only the small calendar or clock icon at the edge. The caret stays where you clicked, so the date can still be typed straight over it, and tabbing through the editor still does not pop pickers open. (#896)
- Hovering the desktop tray icon now shows today's Focus — a count plus as many task titles as fit — instead of an empty tooltip. It updates as Focus changes and needs no setting. Windows limits tray tooltips to 127 characters, so longer lists are trimmed with a trailing ellipsis; Linux desktops do not support tray tooltips at all, so nothing changes there. (#935)
- Time Spent now accepts any whole number of minutes. The field stepped in fives, which also told the browser that only multiples of five were valid, so entering a real figure like 7 or 23 minutes was rejected as an invalid value. Both the task editor and the completion-time dialog now step by one and accept anything you type. (#896)
- The completion-time dialog now uses the same date control as the task editor — the mini calendar with Today/Tomorrow/+2 days suggestions — instead of the browser's own datetime widget, so picking a completion time looks and behaves like every other date field. Reached from the Done badge, a right-click on Done, and the Archive view's Completed line. (#944)
- Importing a `.txt` file or pasting a long list into desktop Quick Add now creates every task in one write instead of one at a time, so an import can no longer stop partway and leave half the list in your inbox — it either all lands or none of it does. If it does fail, the message now names the reason instead of only saying tasks could not be created. (#942)
- Adding tasks to a project on mobile no longer piles up back history. Each saved task left an extra copy of the project screen behind it, so after adding five tasks it took five taps of Back to leave the project — and the same count again to get out of Projects. Saving now returns to the screen you came from, with the project still open. (#938)
- Delete confirmations on desktop no longer run off the bottom of the screen. The dialog shows the task or project name as its heading, so a very long name — a pasted paragraph, say — pushed the Cancel and Delete buttons out of view with no way to scroll to them. The dialog is now height-capped and its text scrolls on its own, with the buttons always visible. Affects Trash and the Archive view's delete confirmations. (#947)
- Restoring a backup now sticks when you use sync. Anything created after the backup was taken still existed on the sync server, so the next sync read its absence as a new record and handed it all back — the restore looked like it worked and then quietly undid itself. Records the restoring device knew about but the backup does not contain are now marked deleted, so the restored snapshot wins everywhere. Records made on another device that this one never saw are left alone. (#939)
- iCloud sync now waits exactly as long as CloudKit asks after a throttle, instead of retrying on a fixed one-minute timer that could trip the same limit again. Rapid edits across two devices could throttle CloudKit, and mobile had no cooldown at all — it retried on the next few-second debounce. Both apps now read the delay CloudKit reports, hold your edits until it passes, and retry automatically; when no delay is given they back off gradually rather than at a fixed interval. (#948)
- The Done list can now be sorted and grouped by when you finished things. **Completion date** is a named sort option there — newest first, with anything never completed last — so it is reachable by name rather than only as the default order, and it applies in Archive too. A **Completion date** grouping splits the list into Today, Yesterday, Previous 7 days, Earlier and Not completed, on your local calendar days, with empty groups hidden. Desktop and mobile; existing defaults are unchanged. (#945)
- The Pomodoro card on mobile can now be folded away. The chevron in its header collapses it to a slim row showing the remaining time and the current phase, so it stops pushing your Focus list down the screen while the timer keeps running. The fold is remembered on that device only rather than syncing, and the card still starts expanded. Desktop already worked this way. (#946)
- Selecting several tasks now offers **Remove tag**, next to Add tag. Until now bulk actions could add a tag and add or remove a context, but the only way to take a tag off was to open each task in turn. The picker lists only the tags the selected tasks actually carry, several can be removed in one pass, and tasks that do not have a picked tag are left untouched. Available wherever bulk selection is — the task lists, saved searches and Review on desktop, and the task lists and Review on mobile — alongside the Contexts screen and project workspace, which already had it. (#949)
- The copy button on a completed task now works. Hovering a row in **Done** or **Archived** shows a copy button, but clicking it did nothing at all — completed rows are read-only, and duplicating was blocked by that same read-only rule, so the one button that only ever appears on completed rows could never run. Duplicate in the right-click menu was dead on completed tasks for the same reason. The app now takes you to the copy rather than leaving it somewhere you cannot see. Desktop only; mobile could already duplicate completed tasks. (#950)
- Duplicating a completed or archived task now puts the copy in your **Inbox** instead of straight onto Next Actions, so work you finished once gets clarified again rather than silently rejoining your actionable list. Its checklist starts unticked and completion is cleared, as before. Duplicating anything else still keeps the original's status — a Someday template stays in Someday. Desktop and mobile. (#950)
- The desktop window now opens at the size and place you left it, without the brief blank window at the default size first: it stays off screen until the saved geometry is applied and the interface has painted. Geometry is also remembered per screen setup — a laptop docked to a large monitor and the same laptop over remote desktop each keep their own size and position, so switching between them no longer means resizing every time. A screen setup seen for the first time starts from your most recent window, shrunk to fit, and leaves the other setups' sizes alone. Kept on the device, never synced. (#936)
- Adding a task to a large project on desktop no longer leaves the screen blank. Once you had scrolled through a project, the row the app scrolls to after a save was being placed past the end of the list, so the view jumped into empty space and only came back when you scrolled by hand. (#916)
- Quick-add syntax no longer stays behind in the task title. Typing `Appeler maman /next` created a Next action called "Appeler maman /next" — the status was applied but the command remained as text. Anything the parser recognizes and applies (`/next`, `/due:`, `@context`, `+Project`, `%person`, `!Area`, tags) now leaves the title on every capture surface, whether or not **Clean up quick add text** is on. That setting keeps its default and now covers only the ambiguous half: whether a date written in ordinary words is also cut from the title. Text that only looks like syntax is untouched — an unknown `/command`, a `TCP/IP` chapter, or a link ending in `/next` all stay exactly as typed.
- Choosing a system calendar on Linux is no longer slow. To work out which Evolution Data Server calendars accept writes, Mindwtr was opening a live connection to every enabled calendar in turn — so one remote account (Exchange, CalDAV, a GNOME Online Accounts calendar) could hold up the whole list for as long as it took to answer. That left the **Push tasks to calendar** switch greyed out on the Calendar settings page, and dragged the rest of the app down because every task edit repeated the same round of connections before pushing. The target list now comes straight from the calendar registry: subscribed feeds like webcal, weather, contacts and birthdays stay out of it, and a calendar that turns out to be shared read-only reports that when a task is pushed to it. Separately, one unreachable calendar account no longer blanks out every other system calendar in the Calendar view. (#575)
- Bulk **Add tag** on mobile now leaves tasks that already carry the tag untouched, and orders tags the way desktop does. (#949)
- iCloud sync no longer gets stuck retrying forever once CloudKit throttles you. Two things kept it going. The apps obeyed the delay CloudKit asked for but never grew it, and CloudKit repeats the same number each time, so a "retry after 23s" became a retry every 23 seconds indefinitely rather than a way out. And on mobile, every switch in and out of the app — plus every change notification from another device — started a sync immediately regardless of the wait already in progress, so the cooldown was set and then skipped seconds later. The wait now doubles each time CloudKit refuses again, up to ten minutes, and a longer delay than that still wins if CloudKit asks for one. Automatic triggers hold off until it passes; **Sync now** in Settings still forces an immediate attempt. (#948)
- Adding a task to a project on mobile no longer leaves that project un-tappable. After saving, the project sheet was gone from the screen while the app still considered it open, so tapping the project again changed nothing and the row looked dead until you visited another screen and came back. The sheet now closes as you go to quick add and reopens when you return, with any title, area, tag or note edits committed on the way out as if you had closed it by hand. (#938)
- Importing a `.txt` file on iOS now actually shows the "Create N tasks?" confirmation instead of appearing to do nothing. The confirmation was a system alert raised while the capture sheet was still open, and iOS never put it on screen, so the file was read and then silently dropped. It now appears inside the capture sheet itself. The same confirmation covers pasting several lines and saving, which could be lost the same way. (#940)
- Pasting several lines into **Add task** on mobile and saving now creates one task per line instead of doing nothing. The "Create N tasks?" confirmation was a system dialog raised over a screen iOS already presents as a sheet, so it never appeared — the save button flashed, no tasks were created, and the invisible dialog left the screen unresponsive until the app was force quit. The confirmation is now drawn on the capture screen itself. (#941)
