- Repeating tasks now show every occurrence across the visible calendar range.
- Focus and Next hide a timed-start task until its exact start time.
- Search labels completed and due dates, and project search covers every project.
- Sync setup validates remote data and attachments before switching; AI model pickers can load the provider's current models.
